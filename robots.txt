# Archive of the old dangerouspress.org site.
# The live site moved to https://dangerouspress.org (served by Cloudflare Workers).
# Disallow crawling here to avoid duplicate-content confusion with the live site.

User-agent: *
Disallow: /
